package com.orange.tgi.ols.arsec.paas.aacm.matrix

import org.scalatest._

class MultiplySpec extends FunSuite with DiagrammedAssertions {
  test("Hello should start with H") {
    assert("Hello".startsWith("H"))
  }
}
